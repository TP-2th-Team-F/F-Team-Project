version: "3"
services:
  database:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: test
      MYSQL_USER: testUser
      MYSQL_PASSWORD: testUser@0805
      MYSQL_ROOT_HOST: root
      MYSQL_ROOT_PASSWORD: aelx0525
    volumes:
      - ./db/data:/var/lib/mysql
#    networks:
#      - test_network
    ports:
      - 3306:3306
    restart: always

#  application:
#    build: .
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/userdb
#    depends_on:
#      - database
#    ports:
#      - 8080:8080
#    restart: always
#    networks:
#      - test_network

#networks:
#  test_network: